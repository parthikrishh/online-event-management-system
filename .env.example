# Frontend runtime API base URL (set in Netlify)
VITE_API_BASE_URL=https://online-event-management-system-134k.onrender.com

# Backend runtime port (set automatically by host, local default is 4000)
PORT=4000
